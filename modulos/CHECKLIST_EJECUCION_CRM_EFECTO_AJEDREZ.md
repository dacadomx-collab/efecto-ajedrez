# Checklist de Ejecución — MODULO_03_CRM_EVENTOS_EN_VIVO aplicado a El Efecto Ajedrez

**Relación:** Instancia concreta de [`MODULO_03_CRM_EVENTOS_EN_VIVO.md`](MODULO_03_CRM_EVENTOS_EN_VIVO.md). Compañero de [`CHECKLIST_EJECUCION_LOGIN_EFECTO_AJEDREZ.md`](CHECKLIST_EJECUCION_LOGIN_EFECTO_AJEDREZ.md) y [`CHECKLIST_EJECUCION_CMS_EFECTO_AJEDREZ.md`](CHECKLIST_EJECUCION_CMS_EFECTO_AJEDREZ.md).

**Autorización:** Dirección explícita del Productor Tzunum, 2026-07-17. `knowledge/` no fue tocado en las Fases 1-9 (bloqueo explícito de `CLAUDE.md` vigente en ese momento) — liberado por el Arquitecto el 2026-07-16 (ver Fase 10) y sincronizado activamente desde entonces.

---

## ⚠️ Desviaciones y correcciones aplicadas sobre el mandato original

1. **Correo de auditoría no quedó como string suelto sin control:** `CORREO_AUDITORIA_STAGING` en `api/email_helper.php` está protegido por `APP_ENV !== 'production'` — en producción esta redirección se ignora automáticamente, nunca puede filtrarse por accidente. El correo original queda visible en el asunto/cuerpo (`[STAGING → originalmente para ...]`) para trazabilidad.
2. **Gap real corregido:** el formulario de captación (punto 4 del mandato) no pedía correo electrónico, pero el punto 5 exige notificación masiva por correo — imposible sin él. Se añadió el campo `email` a `registro_interesados` y al formulario público **antes** de migrar la tabla (no se necesitó una migración `ALTER` separada).
3. **"Tabla ARF-Grid" para el ledger:** se implementó como `<table>` HTML real envuelta en `overflow-x: auto`, no como grid de tarjetas — ARF-Grid es el patrón para bloques repetitivos tipo tarjeta (ya documentado en MODULO_01 §4.3/§5.3); forzarlo sobre datos tabulares con 8 columnas habría sido menos accesible y menos correcto semánticamente. Documentado explícitamente en MODULO_03 §5.
4. **Enlace de la sesión ya no vive hardcodeado en el JS del cliente:** se eliminó la constante `CLUB_MEET_URL` de `main.js` (una debilidad heredada — el "gate" de 15 minutos antes solo era visual, el enlace ya era legible en el código fuente). Ahora se resuelve en `api/sesion_actual.php`, servidor-autoritativo sobre la ventana de tiempo — mejora de seguridad real, no solo paridad funcional.

---

## ✅ Checklist de Desarrollo Operativo

### Fase 1 — Documentación
- [x] `modulos/MODULO_03_CRM_EVENTOS_EN_VIVO.md` — blueprint genérico nuevo.
- [x] `MODULO_01_LOGIN_Y_ACCESO.md` §5.3.1 — patrón de Navegación en Acordeón (Anti-Crowding Sidebar) documentado de forma agnóstica, enlazado desde el índice de módulos relacionados.

### Fase 2 — Schema
- [x] `database/create_table_registro_interesados.sql` (con `email` incluido desde el diseño) — migrada.
- [x] `database/create_table_historial_sesiones.sql` (+ `historial_sesiones_asistentes` con FKs `ON DELETE CASCADE`) — migrada.
- [x] `database/seed_permisos_modulos_invitados.sql` — nuevo módulo `invitados` en la matriz dinámica — migrada.

### Fase 3 — Backend (patrón de 6 capas)
- [x] `api/auth_helpers.php` — `resolverGeoIp()` (ip-api.com, sin credenciales, fallback `NULL` si falla — nunca bloquea el registro).
- [x] `api/registro_interesado.php` — público, captura lead + IP/geo silenciosa.
- [x] `api/sesiones_compartir.php` — vincula todos los interesados vigentes, genera tokens de Check-In únicos, envía notificación individual (nunca el enlace crudo).
- [x] `api/sesion_actual.php` — lectura pública, ventana de tiempo resuelta server-side.
- [x] `checkin.php` — Live Gate: valida token, marca asistencia de forma idempotente, redirige `302` al enlace real.
- [x] `api/email_helper.php` — redirección de staging a `dacadomx@yahoo.com`, gateada por `APP_ENV`.
- [x] `api/permisos_modulos.php` — `MODULOS_VALIDOS` extendido con `invitados`.

### Fase 4 — Frontend
- [x] `dashboard.php` — navegación en acordeón (2 grupos: "Gestión de Accesos", "Círculo de Lectura"; Seguridad/Permisos quedan como ítems planos); panel "Invitados Confirmados" (orquestador + ledger + historial de sesiones); tarjeta `invitados` en la matriz de permisos del `super_admin`.
- [x] `club-lectura.php` — botón "Quiero pertenecer" + modal glassmorphic (`nombre`, `email`, `edad`, `ciudad`, `estado`); lógica del enlace en vivo migrada a `api/sesion_actual.php`.
- [x] `api/logo_test.php` — filtrado a los 4 `logo*.png` reales (antes escaneaba cualquier imagen, incluida la foto de Pao); añadida zona de comparación en modo claro y el toggle Día/Noche interactivo (`initThemeToggle()` reutilizado).
- [x] `assets/css/main.css` — acordeón, tabla ledger, modal de leads, zona clara del laboratorio de logos.
- [x] `assets/js/main.js` — `initAccordionNav()`, `initSesionCompartirForm()`, `initLeadCaptureModal()`; `initClubLectura()` actualizado para resolver el enlace vía fetch en vez de la constante hardcodeada.
- [x] `favicon.ico` verificado presente en los 9 encabezados HTML del proyecto (incluidos los 3 archivos nuevos).

### Fase 5 — Verificación end-to-end (cuentas de prueba temporales, creadas y eliminadas en esta sesión)
- [x] `php -l` (10 archivos) y `node --check` — sin errores.
- [x] Registro público de interesado → `201`, IP `::1` (localhost) correctamente resuelta a geo `NULL` (degradación limpia, sin datos falsos).
- [x] `sesiones_compartir.php` → sesión creada, 1/1 interesados notificados (correo real enviado, interceptado por el redirect de staging).
- [x] `sesion_actual.php` fuera de la ventana de tiempo → `enlace: null` (confirmado con la sesión programada a futuro).
- [x] `checkin.php` con token real → `302` al enlace correcto; segundo clic al mismo token → mismo `302`, sin duplicar el registro de asistencia (verificado 1 sola fila con `checkin_en` no nulo en BD).
- [x] Dashboard con `super_admin`: acordeón, ledger e historial presentes y con datos reales.
- [x] Dashboard con `admin` sin permiso `invitados` habilitado: sección `#invitados` ausente del HTML (0 coincidencias).
- [x] Laboratorio de logos: los 4 archivos `logo*.png`, la zona clara y el toggle confirmados en el HTML servido.
- [x] Limpieza final: 1 solo usuario (cuenta raíz real), `registro_interesados`/`historial_sesiones` en 0 filas, matriz de permisos revertida a sus defaults.

### Fase 6 — Pre-comprobación de tablas y refinamientos (2026-07-17, quinta pasada — dirección "Productor Tzunum")
- [x] **Protocolo de pre-comprobación ejecutado primero, como se pidió:** las 10 tablas del ecosistema (incluidas `invitaciones` y `permisos_modulos`, mencionadas explícitamente como sospechosas del bug de delimitador) se verificaron consolidadas en la BD remota, con los 2 triggers de `log_actividad` intactos y las 4 filas de `permisos_modulos` sembradas correctamente. **No se encontró ninguna inconsistencia** — no fue necesario re-ejecutar ninguna migración.
- [x] Auditoría rápida confirmó que el acordeón, el toggle Día/Noche de `logo_test.php`, la redirección de correo en staging, el modal de captación con geolocalización y el ledger de "Invitados Confirmados" **ya estaban construidos y funcionando** desde el hito anterior — no se reconstruyó nada de eso, solo se verificó su presencia real en el código (no solo en el reporte previo).
- [x] **Selector de fecha manual añadido** a `sesiones_compartir.php` y al formulario del orquestador (`datetime-local`) — si el admin no la especifica, se conserva el cálculo automático como respaldo. Probado con fecha explícita `2026-07-18 20:30:00`, confirmada tal cual en la respuesta y en BD.
- [x] **Carga Protegida de Material (PDF Uploader) — construida de cero:**
  - `database/alter_table_historial_sesiones_add_material.sql` — migrada.
  - `uploads/materiales-protegidos/.htaccess` — `Require all denied`, directorio nuevo sin acceso directo por URL.
  - `api/material_subir.php` — MIME real (`application/pdf` únicamente), 20MB máx., renombrado criptográfico.
  - `api/material_descargar.php` — autorización por `token_checkin` + `checkin_en IS NOT NULL` (asistencia real, no solo interés inicial), verificación de *path traversal* vía `realpath()`.
  - `checkin.php` — dejó de ser una redirección `302` instantánea; ahora es una página de aterrizaje real con "Ingresar a la sesión" y (si hay material) "Descargar material de la sesión".
  - **Probado end-to-end con datos reales:** descarga rechazada (`404`) antes del check-in; tras pasar por `checkin.php`, la misma descarga responde `200` con el PDF real. Limpieza posterior confirmada (archivo físico eliminado, BD en 0 filas de sesiones/interesados, 1 solo usuario real).
- [x] `modulos/MODULO_03_CRM_EVENTOS_EN_VIVO.md` actualizado con la Sección 7 (Carga Protegida de Material) y ajustes en §3.1/§4 para la fecha manual y la Sala de Check-In como landing real (no redirección ciega).

### Fase 7 — Pendiente (no construido en esta pasada, requiere decisión adicional)
- [ ] Diseño visual final del modal de captación de leads (hoy reutiliza `.club-modal`/`.lead-form` — funcional, sin refinamiento adicional de copy/estilo específico para este formulario).
- [ ] Notificaciones de fallo de envío individual visibles en el Dashboard (hoy: el conteo `X/Y notificados` es la única señal; no hay detalle de qué destinatario falló).
- [ ] Borrado del PDF anterior al subir uno nuevo para la misma sesión (hoy: el `UPDATE` solo cambia la referencia en BD; el archivo físico previo queda huérfano en `uploads/materiales-protegidos/`).

### Fase 8 — UX, i18n y enlace roto (2026-07-17, sexta pasada — dirección "Productor Tzunum")
- [x] **Enlace roto resuelto:** `club-lectura.html` recreado como puente fail-safe (meta-refresh + `window.location.replace`) hacia `club-lectura.php` — cero fricción visible, `200` confirmado por HTTP. Documentado como patrón genérico en MODULO_02 §5.5.
- [x] **Horarios internacionales:** agregadas 3 filas con banderas (🇲🇽 CDMX 8:30pm, 🇵🇪 Perú 9:30pm, 🇦🇷 Argentina 11:30pm — calculadas por diferencia horaria real, sin DST en ninguno de los tres países) en la sección de Detalles.
- [x] **Limpieza de botones:** de 3 CTAs con textos distintos a exactamente 2, ambos con el texto literal "Quiero unirme al Club de Lectura" — uno debajo de "Detalles de nuestras reuniones", otro al cierre. Ambos abren el mismo modal de captación (antes solo uno lo hacía). Verificado por HTTP: exactamente 2 coincidencias de la clase+texto exacto.
- [x] **Dashboard completamente seccionado:** las 5 secciones operativas (`usuarios`, `landing`, `invitados`, `seguridad`, `permisos`) nacen con el atributo `hidden` — la pantalla de inicio solo muestra el bloque AURA. Nuevo conmutador `initDashPanelSwitcher()` (`data-panel-target`) revela paneles bajo demanda al navegar el acordeón, con `preventDefault()` sobre el salto de ancla nativo. Verificado por HTTP con cuenta de prueba: las 5 secciones confirmadas `hidden` en el HTML servido tras el login. Documentado como MODULO_01 §5.3.2.
- [x] **Plantilla de invitación despachada a Yahoo real:** se disparó una invitación de prueba a nombre de "Paola Palomares" (rol `admin`) — `correo_enviado: true`, confirmado en `log_actividad`. **Nota importante:** se usó un correo placeholder (`paola.palomares.preview@efecto-ajedrez.local`) porque no se cuenta con el correo real de Paola; el envío SMTP real fue interceptado automáticamente hacia `dacadomx@yahoo.com` por la regla de staging ya activa (Fase 6). La cuenta de prueba se eliminó después — **no se creó su cuenta definitiva**, que requiere su correo real cuando el Arquitecto lo confirme.
- [x] Punto 5 del mandato (automatización de sesiones con horario fijo Mar/Jue, PDF Uploader, filtro de descarga por Check-In, `historial_sesiones` inmutable) — **ya estaba completo desde la Fase 6**; se verificó que sigue funcionando sin tocarlo de nuevo.
- [x] `php -l` / `node --check` sin errores en todos los archivos tocados. BD limpia al cierre: 1 solo usuario real.

### Fase 9 — Re-Ingeniería del Dashboard, Anti-SPAM Universal, i18n y Depuración de Ledgers (2026-07-16, séptima pasada — dirección "Productor Tzunum")
- [x] **`knowledge/` NO fue tocado** — prohibición absoluta explícita en `CLAUDE.md`, sin excepción por instrucción de sesión (mismo precedente de la Fase 8 del checklist de LOGIN).
- [x] **Anti-SPAM universal (no solo la plantilla premium):** `api/email_helper.php` (`enviarCorreoTransaccional()`) agrega ahora las cabeceras `List-Unsubscribe` + `List-Unsubscribe-Post` a **todo** correo saliente, incluidos los correos de notificación de sesión (antes solo un `<p>` simple, sin ese blindaje). Documentado genéricamente en MODULO_01 §2.4.
- [x] **Validación inflexible de unicidad de correo** en `api/registro_interesado.php`: `SELECT` previo por `email` antes del `INSERT`; si ya existe, se omite silenciosamente y la respuesta HTTP es idéntica a la de un alta exitosa (anti-enumeración). Documentado en MODULO_03 §2.
- [x] **Planeador Live unificado:** `api/sesiones_compartir.php` pasó de `application/json` a `multipart/form-data` — un solo endpoint recibe enlace, fecha, tema, mensaje personalizado y PDF opcional en una sola petición; el material se guarda dentro de la misma transacción que crea la sesión (rollback conjunto si el archivo falla). `api/material_subir.php` se conserva intacto como endpoint standalone para reemplazar material sin re-notificar. Documentado en MODULO_03 §3.1.
- [x] **Dos endpoints nuevos para el Registro de Ingreso** (`api/registro_ingreso_listar.php` GET paginado+buscador, `api/registro_ingreso_eliminar.php` POST con 3 modos: `seleccionados`/`cantidad`/`todos`) — reemplazan el `LIMIT 50` server-side fijo que existía antes. Documentado genéricamente en MODULO_01 §9.6.
- [x] **`dashboard.php` reestructurado en jerarquía de widgets** dentro del panel "Invitados Confirmados": Sección A (Planeador Live, único formulario/botón "Compartir"), Sección B (4 tarjetas KPI: fecha programada, libro activo, participantes confirmados, total de usuarios del sistema), Sección C (Historial de sesiones + Interesados registrados, compactos al final). El panel "Registro de Ingreso" (dentro de `#usuarios`) ahora es 100% AJAX: buscador con debounce, paginación de 15 en 15, checkboxes de selección + 4 acciones de borrado (10/15/todos/seleccionados). Documentado en MODULO_01 §5.3.3.
- [x] **Formularios viejos retirados sin dejar código muerto:** `#sesion-compartir-form` y `#material-subir-form` (y sus `initSesionCompartirForm()`/`initMaterialSubirForm()` en `main.js`) eliminados — su función quedó absorbida por `#planeador-live-form`/`initPlaneadorLiveForm()`. Confirmado por grep: cero referencias residuales en `.php`/`.js`.
- [x] **Ledger de Interesados aligerado:** columnas `IP` y `Ubicación (IP)` retiradas del `<table>` visible en `dashboard.php` — los datos se siguen consultando y persistiendo en `registro_interesados` para auditoría interna, solo se dejaron de renderizar. Documentado en MODULO_03 §5.
- [x] **`club-lectura.php` — husos horarios consolidados** en un solo cuadro (`.club-schedule-card`) con las 3 banderas/horarios (🇲🇽/🇵🇪/🇦🇷) en vez de 3 tarjetas `.club-details__item` separadas; se conservan los mismos `data-block-id` para no romper el Motor de Edición Visual. El copy instructivo de la ventana de 15 minutos ya existía tal cual en `detalles_lead` — verificado, sin cambios necesarios.
- [x] **Enrutador dinámico de acceso condicional:** el modal de registro (`registro-interesado-form`) marca `localStorage.clubLecturaConfirmado = '1'` al confirmar con éxito; el botón "Ingresar a la Sala de Check-In" y el auto-popup del modal en vivo (`club-modal`) ahora exigen **ambas** condiciones — ventana de tiempo (ya existente) **y** esa confirmación previa (nueva) — antes solo exigían la ventana de tiempo. Sin confirmar, se muestra un texto instructivo ("Regístrate para desbloquear tu acceso"). Documentado en MODULO_03 §3.2.
- [x] Verificado con `php -l` (`dashboard.php`, `club-lectura.php`, `api/sesiones_compartir.php`, `api/registro_interesado.php`, `api/email_helper.php`, `api/registro_ingreso_listar.php`, `api/registro_ingreso_eliminar.php`) y `node --check` (`assets/js/main.js`) — sin errores. `index.php` revisado — su único enlace a `club-lectura.php` sigue intacto, sin referencias rotas.
- [x] MODULO_01_LOGIN_Y_ACCESO.md actualizado: §2.4 (Ley de Oro Anti-SPAM universal), §5.3.3 (jerarquía de widgets Acción→Estado→Historial), §9.6 (ledgers paginados). MODULO_03_CRM_EVENTOS_EN_VIVO.md actualizado: §2 (unicidad de correo + gate de confirmación), §3.1 (planeador unificado), §3.2 (doble gate de acceso), §3.3 (nueva — husos horarios consolidados), §5 (columnas ocultas + referencia a ledgers paginados), §8 (3 filas nuevas de validación). `FUENTEDEVERDAD_CONSOLIDADA.md` §6 (nueva — índice de blueprints reutilizables del holding).

### Fase 10 — Consolidación Estética, Liberación de `knowledge/` y Plantilla Ejecutiva de Correo (2026-07-16, octava pasada — dirección "Productor Tzunum")
- [x] **`CLAUDE.md` revisado, sin cambio necesario:** la "Regla de Hierro" ya estaba acotada exclusivamente a `knowledge/info.txt` (no a la carpeta completa) al momento de leer el archivo en esta pasada — confirmado por lectura directa, no por memoria de la pasada anterior. `knowledge/` queda disponible para mantenimiento vivo del agente (Mandamiento 17), con `info.txt` como única excepción absoluta.
- [x] **Plantilla premium de correo — rediseño ejecutivo:** `api/email_helper.php` (`construirPlantillaInvitacion()`) — jerarquía tipográfica aumentada en todo el cuerpo (h1: 20→25px, párrafos: 15→17px, botón CTA: 15→17px, enlaces secundarios: 13→15px, footer legal: 10→11px). Logo con URL absoluta vía `APP_URL` y nombre dinámico del destinatario ya estaban correctos (verificado, sin hardcodeo). Footer de dirección postal reemplazado: el placeholder "[pendiente de confirmar]" se sustituyó por una referencia formal a la identidad del proyecto — **no se fabricó una dirección postal física real** (Mandamiento 4, Anti-Alucinación); la dirección física real para cumplimiento CAN-SPAM completo sigue pendiente de que el Arquitecto la provea.
- [x] **Correo real de auditoría enviado:** cuenta `super_admin` temporal creada vía script CLI directo (`password_hash()` + `Database`, sin pasar por HTTP porque no había sesión real disponible), login real vía `api/login.php`, disparo de `api/staging_test_invitacion.php` con la sesión obtenida — `status: success`, correo despachado a `dacadomx@yahoo.com` (redirección automática de staging, `APP_ENV=local`). Cuenta QA eliminada inmediatamente después (`DELETE` directo, verificado: 3 usuarios reales restantes, sin residuos).
- [x] **Planeador Live — pulido de campos:** reordenado a enlace → fecha → tema/mensaje → PDF (antes el PDF iba primero); etiqueta del campo "tema" renombrada a "Tema del libro / Mensaje rápido" para coincidir con el copy solicitado. La reestructuración en Secciones A/B/C ya estaba construida desde la Fase 9 — confirmado sin necesidad de reconstruirla.
- [x] **club-lectura.php — copy destacado dentro del cuadro unificado:** el texto "Enlace/Link Google Meet — el acceso aparece aquí 15 min antes" se movió de un `<li>` separado a una nota (`.club-schedule-card__note`, borde superior + color de acento) dentro del mismo `.club-schedule-card` que ya agrupaba los 3 husos horarios desde la Fase 9 — ahora es un único bloque visual completo, no dos.
- [x] **Ocultamiento de columnas y unicidad de leads — ya cumplidos desde la Fase 9, confirmado sin cambios:** verificado por grep que `dashboard.php` no renderiza `<th>IP</th>`/`<th>Ubicación (IP)</th>` en la tabla de Interesados, y que `api/registro_interesado.php` ya valida unicidad de correo contra `registro_interesados` (no existe tabla `leads_captura` en este proyecto — aclarado de nuevo, ver Fase 9).
- [x] **`knowledge/` — backfill acotado, no exhaustivo:** se añadieron notas de alcance + entradas nuevas en `knowledge/02_CODEX_Y_SCHEMA_MAESTRO.md` (endpoints de esta pasada) y `knowledge/03_CONTRATOS_API_Y_RUTAS.md` (contratos de `registro_ingreso_listar.php`, `registro_ingreso_eliminar.php`, `sesiones_compartir.php` actualizado, `registro_interesado.php`), y dos entradas de pantalla real en `knowledge/07_UI_MODULOS_Y_PANTALLAS.md` (Dashboard, Club de Lectura). El backfill histórico completo del schema/contratos del sistema de Login/CRM (construido en pasadas previas mientras `knowledge/` estaba bloqueado) queda flageado explícitamente como pendiente — no se fabricó de golpe sin re-verificar cada endpoint contra el código real.
- [x] Verificado con `php -l` (`dashboard.php`, `club-lectura.php`, `api/email_helper.php`) — sin errores. Sin cambios en `assets/js/main.js` esta pasada (la lógica ya existía desde la Fase 9).

### Fase 11 — Auditoría RBAC, Dinamización Reforzada del Correo y Sincronización a `knowledge/` (2026-07-16, novena pasada — dirección "Productor Tzunum")
- [x] **RBAC del Registro de Ingreso — auditado, sin regresión:** verificado línea por línea que `dashboard.php` ya ocultaba el bloque completo tras `$esSuperAdmin` (no `esModuloVisible()`, que sí es delegable a `admin`) y que `api/registro_ingreso_listar.php`/`api/registro_ingreso_eliminar.php` ya exigían `requireAuth($pdo, ['super_admin'])` desde la Fase 9. Sin cambios de código — el mandato pedía "modificar", pero la restricción ya era correcta; se documentó la verificación en vez de tocar código que ya cumplía.
- [x] **Dinamización reforzada del correo:** `construirPlantillaInvitacion()` gana un tercer parámetro `$email` — ahora el cuerpo del correo muestra explícitamente "Esta invitación fue generada exclusivamente para: {email real}", visible incluso cuando el transporte se desvía a staging. Actualizados los 2 llamadores reales (`api/usuarios_invitar.php`, `api/usuarios_resetear_password.php`); `api/staging_test_invitacion.php` deliberadamente no lo pasa (no hay destinatario real en la previsualización).
- [x] **Escala tipográfica aumentada de nuevo** (h1: 25→26px, cuerpo: 17→18px, botón: 17→18px) y footer CAN-SPAM reescrito como bloque de 3 líneas tipo dirección postal (nombre del proyecto + descripción + "México, LatAm") — sigue sin fabricar una calle/número reales (Mandamiento 4); la dirección física verificable sigue pendiente del Arquitecto.
- [x] **Orden del Planeador Live (dashboard.php) — auditado, sin cambio necesario:** ya estaba en el orden solicitado (enlace → fecha → tema/mensaje → PDF → Compartir) y ya era lo primero visible al abrir "Invitados Confirmados", seguido de KPIs y luego el historial — construido en la Fase 9, pulido en la Fase 10.
- [x] **Correo real de auditoría despachado de nuevo** con la plantilla reforzada: cuenta `super_admin` temporal (creación vía script CLI directo + login real + `api/staging_test_invitacion.php`), `status: success`, correo a `dacadomx@yahoo.com`. Cuenta QA eliminada, verificado: 3 usuarios reales restantes.
- [x] **`favicon.ico` confirmado presente** en los 10 archivos `.php` que renderizan `<head>` (login, setup-genesis, invitacion, recuperar/restablecer-password, index, checkin, club-lectura, dashboard, logo_test) — sin huecos.
- [x] **Sincronización `modulos/` → `knowledge/` (segunda ronda, acotada):** `knowledge/02_CODEX_Y_SCHEMA_MAESTRO.md` y `knowledge/03_CONTRATOS_API_Y_RUTAS.md` ampliados con la firma nueva de `construirPlantillaInvitacion()` y la re-confirmación del RBAC. `FUENTEDEVERDAD_CONSOLIDADA.md` §8 (nueva) — tabla de estado verificado código↔`knowledge/` para todo el hito de Accesos y Eventos en Vivo, con el backfill histórico completo marcado explícitamente como pendiente (no fabricado).
- [x] **MODULO_01_LOGIN_Y_ACCESO.md → v7.0:** auditado para blindaje 100% agnóstico — renombrado el título de §9.4 (quitada la referencia a la identidad de marca "Trinchera Nocturna", específica de este proyecto), añadido `{{USER_EMAIL}}` en la dinamización del correo (§9.4), reforzada §9.3 con el patrón de "módulo de rol fijo no-delegable" (defensa en profundidad UI+backend), footer anti-SPAM reescrito para no asumir que existe una dirección postal real disponible. Grep de cierre confirmó cero menciones de "Ajedrez", "Mentores", "Pao", dominios o correos reales en el documento.
- [x] Verificado con `php -l` (`dashboard.php`, `club-lectura.php`, `api/email_helper.php`, `api/usuarios_invitar.php`, `api/usuarios_resetear_password.php`, `api/registro_ingreso_listar.php`, `api/registro_ingreso_eliminar.php`, `api/staging_test_invitacion.php`) y `node --check` (`assets/js/main.js`) — sin errores.

### Fase 12 — Formato de Fecha Ajustado, Dirección Real Confirmada y Auditoría Final (2026-07-16, décima pasada — dirección "Productor Tzunum")
- [x] **Formato del Registro de Ingreso corregido:** `formatearFechaMazatlan()` cambiado de "16 Jul 2026, 8:26 pm" a "Jul 16, 2026, 08:31 pm" (mes-día-año, día/hora con 2 dígitos) — mismo huso `America/Mazatlan`, mismo arreglo de meses en español. Probado por CLI: `2026-07-17 03:31:00` UTC → `Jul 16, 2026, 08:31 pm`; `2026-01-05 15:05:00` UTC → `Ene 05, 2026, 08:05 am`.
- [x] **Dirección física del footer — región real confirmada, no fabricada:** el Productor Tzunum proveyó explícitamente "La Paz, Baja California Sur, México" en esta pasada; se incorporó al bloque del footer (reemplazando el genérico "México, LatAm" de la Fase 10). Sigue sin incluir calle/número porque esos datos no fueron provistos — la distinción entre "región confirmada por el dueño del proyecto" (usable) y "dato inventado por el agente" (prohibido, Mandamiento 4) quedó documentada explícitamente en MODULO_01 §9.4.
- [x] **Escala tipográfica — tercera ronda de refuerzo:** h1 26→28px, cuerpo 18→19px, botón 18→19px, tagline 16→17px, enlaces secundarios 16→17px, colibrí/copyright/footer legal +1px cada uno. Ya en el techo razonable para un contenedor de correo de 480px de ancho — más allá de esto se arriesga a romper el layout en clientes de correo angostos.
- [x] **RBAC, orden del dashboard, cuadro único de horarios y unicidad de correo — auditados de nuevo, sin cambios de código:** todos ya cumplían desde pasadas anteriores (Fases 9-11). Aclarado en esta pasada que la "unicidad por sesión" pedida en el mandato no aplica al modelo de datos actual — `registro_interesados` es una lista de interesados perenne (sin `sesion_id`), notificada de *todas* las sesiones futuras vía `sesiones_compartir.php`; la unicidad ya implementada es global por correo, que es la interpretación correcta para ese diseño (crear un concepto de "registro por sesión" sería una alteración de schema no autorizada, Mandamiento 9).
- [x] **Auditoría de código muerto:** revisadas las 40 funciones de `assets/js/main.js` — todas siguen siendo invocadas desde `DOMContentLoaded` o desde otra función; ninguna huérfana tras los retiros de `initSesionCompartirForm()`/`initMaterialSubirForm()` en la Fase 9.
- [x] **Correo real de auditoría despachado con la plantilla final de esta pasada:** cuenta `super_admin` temporal, login real, `api/staging_test_invitacion.php` → `status: success`, correo a `dacadomx@yahoo.com`. Cuenta QA eliminada, verificado: 3 usuarios reales restantes.
- [x] `knowledge/02_CODEX_Y_SCHEMA_MAESTRO.md` y `FUENTEDEVERDAD_CONSOLIDADA.md` §8 ampliados con el nuevo formato de fecha y la dirección confirmada. `MODULO_01_LOGIN_Y_ACCESO.md` → **v8.0**: §9.3 generaliza el formato de fecha como configurable por el dueño del proyecto (ya no asume un único orden "correcto"), §9.4 documenta la distinción "región confirmada vs. dato inventado". Grep de cierre confirmó cero menciones de marca, incluida la nueva región real.
- [x] Verificado con `php -l` (`api/auth_helpers.php`, `api/email_helper.php`, `dashboard.php`, `club-lectura.php`, `api/registro_interesado.php`, `api/registro_ingreso_listar.php`, `api/registro_ingreso_eliminar.php`, `api/usuarios_invitar.php`, `api/usuarios_resetear_password.php`) y `node --check` (`assets/js/main.js`) — sin errores.
